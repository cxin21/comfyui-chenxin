# Vendored tokenizer snapshot for minimax-h3-prompt.
# Hashes pinned in knowledge/manifest.json were computed against LF content;
# force LF on checkout so the integrity gate stays green across platforms.
skills/minimax-h3-prompt/knowledge/** text eol=lf
