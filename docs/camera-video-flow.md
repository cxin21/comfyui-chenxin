# Camera video flow

`camera-video` consumes `{"prompt": {"text": "..."}}` plus numeric duration
and stage-specific reference images. It owns the fixed MiniMax H3 workflows,
reference uploads, timeline runtime settings, and output downloads.
