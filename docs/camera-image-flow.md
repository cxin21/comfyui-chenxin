# Camera image flow

`camera-image` consumes an envelope shaped as
`{"prompt": {"positive": "...", "negative": "..."}}`. It owns the fixed
Anima workflow, camera settings, image size, sampling, groups, uploads, and
the i2i reference path.
